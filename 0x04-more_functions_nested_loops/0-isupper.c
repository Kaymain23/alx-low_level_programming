#include "main.h"
/**
*_isupper - function that verifies if a  character is uppercase or not
*@c: tested character
*Return: returns 1 if cis uppercase , 0 if not otherwise
*/

int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))
return (0);
}
